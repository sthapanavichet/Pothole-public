# Dashboard environment
# Copy to .env.local if you want to override the default API URL / keys.

# Shared backend used by Streamlit + dashboard (Vercel API → Supabase)
VITE_API_URL=https://api-mu-ten-54.vercel.app

# Must match API_READ_KEY on the Vercel API (visible in the browser bundle)
VITE_API_READ_KEY=replace_with_same_read_key_as_api
