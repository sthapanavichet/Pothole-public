# Streamlit / YOLO backend environment
# Copy to backend/.env (or export in your shell) before posting detections.

POTHOLE_API_URL=https://api-mu-ten-54.vercel.app

# Must match API_WRITE_KEY on the Vercel API. Never put this in the dashboard.
POTHOLE_API_WRITE_KEY=replace_with_same_write_key_as_api
