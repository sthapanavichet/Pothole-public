# Build Instructions (LaTeX)

This project uses LaTeX source at:
- `submission/project_integration_submission.tex`

Compile command (when TeX is installed):
- `pdflatex -interaction=nonstopmode -output-directory submission submission/project_integration_submission.tex`

Run twice for stable references/table of contents.
